class('java.lang.Integer':
  def('+' | other:
    'java.lang.Math'.addExact(this, other)
  )
)

class(Counter:
  def(next:
    c = count()
    Counter.def(count: c + 1)
    c
  )
  
  def(count: 0)
)

counter = Counter()
counter.next() + counter.next()
